/// <summary>
/// Counters recorded globally via StatisticsManager.AddStatistic.
/// Extend this enum as gameplay systems need new tracked values.
/// </summary>
public enum PlayerStatistic
{
	None = 0,
	EnemiesKilled = 1,
	IngredientsCollected = 2,
}
